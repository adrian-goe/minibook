query GetAuthors {
  getAuthors {
    id
    name
  }
}

mutation CreateBook($createBook: BookCreateInput!) {
  createBook(createBook: $createBook) {
    author {
      name
    }
    isbn
    name
    id
  }
}
